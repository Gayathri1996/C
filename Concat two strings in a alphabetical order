#include<stdio.h>
#include <stdlib.h>
#include<string.h>

int main()
{
char str1[100],str2[100];
int index1,index2;
scanf("%s",str1);
scanf("%s",str2);
index1=str1[0]-'a';
index2=str2[0]-'a';
if(index1<index2)
{
    printf("%s",strcat(str1,str2));
}
else
{
    printf("%s",strcat(str2,str1));
}
}
